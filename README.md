# Info

This repository contains spaceship-prompt (https://github.com/denysdovhan/spaceship-prompt) as submodule.
Submodule is located in `.zsh/spaceship-prompt`.

When cloning use the following commands:

```bash
git clone https://github.com/lukakudra/dotfiles.git
```

```bash
git submodule update --init --recursive
```
